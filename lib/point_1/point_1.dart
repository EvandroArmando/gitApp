import 'package:flutter/material.dart';
    
class Point1 extends StatelessWidget {

  const Point1({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}